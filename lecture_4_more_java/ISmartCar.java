
// Note that for naming interfaces in java there is no convention
// But, generally name can be adjective, i.e. Comparable
// With polymorphism interface declares a supertype

public interface ISmartCar {

    Double version = 1.0;

    void navigate(String destination);
    void park();
}
