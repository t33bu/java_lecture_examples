
public class App {
    public static void main(String[] args) throws Exception {
        
        /* 
        // Vehiclefactory
        Vehicle myCar = VehicleFactory.produce("sedan");
        // Access object through the interface
        myCar.start();
        myCar.drive();
        myCar.stop();

        // Databse singleton
        Database myDB = Database.getInstance();        
        myDB.getConnection();        

        // News observer
        NewsAgency na = new NewsAgency();
        RadioNews radio = new RadioNews();
        TVNews tv = new TVNews();
        // register observers
        na.registerObserver(radio);
        na.registerObserver(tv);
        // publishing news automatically calls update
        // in observers
        na.notify("Breaking! Java course about to end!");        

        TemperatureSensor ts = new TemperatureSensor();
        ts.activate();
        System.out.println("Sensor measured " + ts.getData() + "C");

        HumiditySensor hs = new HumiditySensor();
        hs.activate();
        System.out.println(hs.getClass().getName() + " measured " + hs.getData() + "%");

        Bike myBike = new Bike(); 
        Vehicle vehicle = new VehicleAdapter(myBike);
        vehicle.start();
        vehicle.drive();
        vehicle.stop();
        */
    }
}

